<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <!-- Preload Google Fonts CSS to reduce CLS -->
    <link
      rel="preload"
      as="style"
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:ital,wght@0,100..700;1,100..700&display=swap"
      onload="this.onload=null;this.rel='stylesheet'"
    />
    <noscript>
      <link
        href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:ital,wght@0,100..700;1,100..700&display=swap"
        rel="stylesheet"
      />
    </noscript>
    <link
      rel="preload"
      as="style"
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:ital,wght@0,100..700;1,100..700&family=Noto+Sans:ital,wght@0,100..900;1,100..900&display=swap"
      onload="this.onload=null;this.rel='stylesheet'"
    />
    <noscript>
      <link
        href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:ital,wght@0,100..700;1,100..700&family=Noto+Sans:ital,wght@0,100..900;1,100..900&display=swap"
        rel="stylesheet"
      />
    </noscript>
    <title>Javier Hengda</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  @import "tailwindcss";
  @plugin "@midudev/tailwind-animations";
  :root {
    /* Color system */
    --color-bg: #100c08; /* near-black brown */
    --color-bg-2: #0c0906; /* deeper tone for gradients */
    --color-fg: #ffffff;
    --color-fg-muted: #e5e7eb;
    --color-accent: #ffe9b1; /* warm gold */
    --color-accent-2: #d9a441;

    /* Surface */
    --surface: rgba(255, 255, 255, 0.04);
    --surface-border: rgba(255, 255, 255, 0.1);

    /* Effects */
    --ring: rgba(255, 233, 177, 0.5);
    --shadow: 0 10px 30px rgba(0, 0, 0, 0.35), 0 2px 10px rgba(0, 0, 0, 0.25);
  }

  * {
    box-sizing: border-box;
  }

  html,
  body {
    margin: 0;
    padding: 0;
    width: 100%;
    min-height: 100vh;
    overflow-x: hidden;
    color-scheme: dark;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  html {
    scroll-behavior: smooth;
    scroll-padding-top: 6rem; /* account for fixed nav */
  }

  body {
    /* Layered background for depth */
    background: radial-gradient(
        1200px 600px at 80% 10%,
        rgba(255, 233, 177, 0.08),
        transparent 60%
      ),
      radial-gradient(
        800px 400px at 10% 20%,
        rgba(255, 233, 177, 0.06),
        transparent 55%
      ),
      linear-gradient(180deg, var(--color-bg) 0%, var(--color-bg-2) 100%);
    background-color: var(--color-bg);
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-family:
      "IBM Plex Sans",
      "Noto Sans",
      system-ui,
      -apple-system,
      sans-serif;
  }
  body,
  p,
  span,
  a,
  li,
  ul,
  ol,
  blockquote,
  pre,
  code,
  textarea,
  input,
  select,
  button {
    font-family:
      "IBM Plex Sans",
      "Noto Sans",
      system-ui,
      -apple-system,
      sans-serif;
  }

  ::selection {
    background: var(--color-accent);
    color: #1a1a1a;
  }

  a {
    text-decoration: none;
    color: inherit;
  }

  /* Utility: glass surface */
  .glass {
    background: var(--surface);
    backdrop-filter: saturate(140%) blur(10px);
    -webkit-backdrop-filter: saturate(140%) blur(10px);
    border: 1px solid var(--surface-border);
    box-shadow: var(--shadow);
  }
</style>
