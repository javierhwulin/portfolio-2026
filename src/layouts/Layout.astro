<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#0a0908" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    
    <!-- SEO Meta Tags -->
    <meta name="description" content="Javier Hengda - Backend Engineer & Full-Stack Developer. Building robust, scalable applications with modern technologies." />
    <meta name="keywords" content="Backend Engineer, Full-Stack Developer, Software Engineer, Java, Python, TypeScript, Vue, Django, SpringBoot" />
    <meta property="og:title" content="Javier Hengda - Backend Engineer" />
    <meta property="og:description" content="Building robust, scalable applications with modern technologies." />
    <meta property="og:type" content="website" />
    
    <!-- Resource hints for external domains -->
    <link rel="dns-prefetch" href="https://fonts.googleapis.com" />
    <link rel="dns-prefetch" href="https://fonts.gstatic.com" />
    <link rel="dns-prefetch" href="https://github.com" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    
    <!-- Non-blocking font loading with preload -->
    <link
      rel="preload"
      as="style"
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=Instrument+Sans:ital,wght@0,400..700;1,400..700&display=swap"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=Instrument+Sans:ital,wght@0,400..700;1,400..700&display=swap"
      rel="stylesheet"
      media="print"
      onload="this.media='all'"
    />
    <noscript>
      <link
        href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=Instrument+Sans:ital,wght@0,400..700;1,400..700&display=swap"
        rel="stylesheet"
      />
    </noscript>
    
    <title>Javier Hengda — Backend Engineer</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  @import "tailwindcss";
  @plugin "@midudev/tailwind-animations";

  :root {
    /* Color system */
    --color-bg: #0a0908;
    --color-bg-2: #050403;
    --color-fg: #ffffff;
    --color-fg-muted: #e5e7eb;
    --color-accent: #ffe9b1;
    --color-accent-2: #d9a441;

    /* Surface */
    --surface: rgba(255, 255, 255, 0.03);
    --surface-border: rgba(255, 255, 255, 0.08);

    /* Effects */
    --ring: rgba(255, 233, 177, 0.5);
    --shadow: 0 10px 30px rgba(0, 0, 0, 0.35), 0 2px 10px rgba(0, 0, 0, 0.25);
  }

  * {
    box-sizing: border-box;
  }

  html,
  body {
    width: 100%;
    min-height: 100vh;
    overflow-x: hidden;
    color-scheme: dark;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  html {
    scroll-behavior: smooth;
    scroll-padding-top: 6rem;
  }

  body {
    font-family: "Instrument Sans", system-ui, -apple-system, sans-serif;
    background-color: var(--color-bg);
    color: var(--color-fg);
  }

  body::before {
    content: "";
    position: fixed;
    inset: 0;
    z-index: -1;
    background-image: 
      radial-gradient(
        ellipse 1000px 600px at 85% 5%,
        rgba(255, 233, 177, 0.06),
        transparent 60%
      ),
      radial-gradient(
        ellipse 700px 500px at 5% 30%,
        rgba(255, 233, 177, 0.04),
        transparent 55%
      ),
      radial-gradient(
        ellipse 600px 400px at 50% 100%,
        rgba(217, 164, 65, 0.03),
        transparent 60%
      );
    pointer-events: none;
  }

  /* Typography */
  h1, h2, h3, h4, h5, h6 {
    font-family: "Instrument Sans", system-ui, -apple-system, sans-serif;
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  p, span, a, li, button {
    font-family: "Instrument Sans", system-ui, -apple-system, sans-serif;
  }

  code, pre, .mono {
    font-family: "IBM Plex Mono", monospace;
  }

  ::selection {
    background: var(--color-accent);
    color: var(--color-bg);
  }

  a {
    text-decoration: none;
    color: inherit;
  }

  /* Section Container */
  .section-container {
    width: 100%;
    max-width: 1280px;
    margin-left: auto;
    margin-right: auto;
    padding-left: 1.5rem;
    padding-right: 1.5rem;
  }

  @media (min-width: 768px) {
    .section-container {
      padding-left: 3rem;
      padding-right: 3rem;
    }
  }

  /* Utility: glass surface */
  .glass {
    background: var(--surface);
    backdrop-filter: saturate(140%) blur(12px);
    -webkit-backdrop-filter: saturate(140%) blur(12px);
    border: 1px solid var(--surface-border);
    box-shadow: var(--shadow);
  }

  /* Smooth fade transitions between sections */
  section {
    position: relative;
    contain: layout style;
  }

  .project-card,
  .service-card,
  .skill-card,
  .stat-item {
    contain: layout style paint;
  }

  /* Custom scrollbar */
  ::-webkit-scrollbar {
    width: 8px;
    height: 8px;
  }

  ::-webkit-scrollbar-track {
    background: transparent;
  }

  ::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.15);
    border-radius: 4px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: rgba(255, 255, 255, 0.25);
  }

  /* Focus visible styles */
  :focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }

  /* Reduced motion preferences */
  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
    
    html {
      scroll-behavior: auto;
    }
  }
</style>
